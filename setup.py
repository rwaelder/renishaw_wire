from setuptools import setup

setup(
   name='renishaw_wire',
   version='0.8',
   description='Library to read Renishaw WiRE (.wdf) files',
   # packages=['fit_spectra'],  
   install_requires=['numpy', 'pandas'], 
)
